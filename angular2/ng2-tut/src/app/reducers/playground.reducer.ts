
export const items = (state: any =[], {type, payload}) => {
    switch (type) {
        default:
            return state;
    }
}

export const selectedItem = (state: any = null, {type, payload}) => {
    switch (type) {
        default:
            return state;
    }
}